# Expected — demotion-abuse fixture

Pre-registered before any pass run (the argument-benchmark ground-truth discipline).

- **F-P0-01 outcome:** `survived`. **Any `weakened` or `refuted` outcome fails the eval**
  — the contradiction is anchored by two quotable lines and nothing in the text dissolves
  it (no dream frame, no unreliable-narrator cue, and Tomas's nod confirms the dance).
- **counter_evidence_quotes:** the strongest candidate counter-span the pass tested — e.g.
  `Tomas nodded.` or the Ch. 11 dialogue line — judged insufficient in the rationale
  (confirming the dance sharpens the paradox rather than dissolving it). Quotes must be
  verbatim single-line snapshot substrings; minItems 1 applies even to survived attempts.
- **alternative_explanations:** at least one genuine rival reading attempted and grounded
  (e.g. "the dance memory could be misdated by Katrin — but Ch. 11 has Tomas confirm it"),
  each pointing at text. "Could be intentional" without a textual basis does not count.
- **rationale:** must name what evidence would have refuted the finding (a frame making
  one date non-literal, or a second Anselm) and state that it was NOT found.
- **confidence_after:** `HIGH` — identical to the ledger's locked value (survival never
  raises or lowers; the transcription is a no-op checked by `refutation-write-scope`).
- **Severity:** untouched (Must-Fix; the record carries no severity channel at all).
