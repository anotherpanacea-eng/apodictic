---
name: apodictic-new-project
description: >
  Codex-generated wrapper for APODICTIC's compatibility command surface. Use when the user explicitly asks for `apodictic-new-project`, or when they say something like "Set up a new editing project.", or when they want initialize a new project with contract and diagnostic state. This wrapper mirrors the legacy `/new-project` behavior.
version: 1.9.0
---

<!-- Generated by scripts/build-codex.mjs. Do not edit files in codex/ by hand. -->

# APODICTIC New Project

This generated skill exposes the legacy `/new-project` workflow in Codex without duplicating the shared APODICTIC runtime.

## Load Order

1. Read `../../commands/new-project.md` as the canonical command behavior.
2. Read `../core-editor/SKILL.md`.
3. Load only the references, companion skills, and output rules that those source files call for.

## Execution Notes

- Preserve the command's routing, output, and resume behavior.
- Skip redundant intake when router output or project state has already prefilled the needed context.
- Follow the folder architecture in `../core-editor/references/output-policy.md` §Folder Architecture: rolling state at the project root, run artifacts in `runs/YYYY-MM-DD_{model}_{type}/`.
