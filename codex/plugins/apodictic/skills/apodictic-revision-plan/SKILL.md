---
name: apodictic-revision-plan
description: >
  Codex-generated wrapper for APODICTIC's compatibility command surface. Use when the user explicitly asks for `apodictic-revision-plan`, or when they want compatibility alias for /coach. This wrapper mirrors the legacy `/revision-plan` behavior.
version: 1.9.1
---

<!-- Generated by scripts/build-codex.mjs. Do not edit files in codex/ by hand. -->

# APODICTIC Revision Plan

This generated skill exposes the legacy `/revision-plan` workflow in Codex without duplicating the shared APODICTIC runtime.

## Load Order

1. Read `../../commands/revision-plan.md` as the canonical command behavior.
2. Read `../revision-coach/SKILL.md`.
3. Load only the references, companion skills, and output rules that those source files call for.

## Execution Notes

- Preserve the command's routing, output, and resume behavior.
- Skip redundant intake when router output or project state has already prefilled the needed context.
- Follow the folder architecture in `../core-editor/references/output-policy.md` §Folder Architecture: rolling state at the project root, run artifacts in `runs/YYYY-MM-DD_{model}_{type}/`.
