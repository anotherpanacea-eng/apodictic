### Onboarding — router diagram in the README

The README now opens with a GitHub-rendered Mermaid flowchart of the `/start` router: the three intake questions (what you have, what you want, what changes how we work) branching to the workflows they route to, converging on the editorial letter ("diagnosis, never a rewrite"). It renders inline on github.com and links out to the interactive route explorer.
