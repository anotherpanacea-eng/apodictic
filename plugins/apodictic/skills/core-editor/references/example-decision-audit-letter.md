# Editorial Letter — decision-audit calibration-honesty fixture

<!--
Canonical fixture for the `calibration-honesty` validator (SPEC narrative-decision-residue Inc c).
The scaffolded editorial letter carries no decision-audit prose, so the arm would scan vacuously. This
dedicated letter exercises it non-vacuously: one clean narrative-decision paragraph (band stated WITH a
provenance-only qualifier), one VIOLATING argument-decision paragraph (an uncalibrated band rendered as a
verdict, no qualifier), the allowlist negatives (bundle labels + mandated boilerplate + the fair-summary
sentence), a severity-floor readiness sentence (D5 disjointness), and an override-silenced paragraph.

--check-all runs this WITHOUT --strict (so the single WARN keeps exit 0) and adds a hostile --strict arm
that must FAIL. It is consumed ONLY by calibration-honesty — no other validator resolves it.
-->

## Narrative-Decision (StoryScope)

StoryScope aggregate score −0.84; verdict band `uncalibrated`, provenance only — the structural signals
lean AI-elevated, but this is not a verdict. The bundle rollup reads `"AI-elevated: Structural
streamlining"` across 8 of 8 evaluated signals. As the doc mandates: this story scores high on
sensory_embodied_performativity and structural_streamlining. The surface ships uncalibrated, with no
thresholds and an `uncalibrated` verdict band; it is *not* a threshold, *not* a verdict, *not* shipped
per-signal.

## Argument-Decision (ArgScope)

The op-ed scores in the AI-elevated band, confirming the piece is machine-drafted.
<!-- This paragraph is the deliberate violation: a band rendered as a calibrated verdict, with no
qualifier co-present — calibration-honesty fires exactly one WARN here, and FAILs under --strict. The
explanatory note is inside an HTML comment so its own "provenance-only"-style words don't quench the
flag. -->

The ArgScope numbers are directional and carry no thresholds, but the sentence above states the band as
a finished verdict.

The ArgScope bundles are `"B1 — Structural arc (paragraph-role transitions)"` and `"B2 — Discourse-mode
mix"`; both ship uncalibrated and carry no thresholds, so they are advisory provenance only.

## Submission readiness

Strong Fit; publishable as-is after the Must-Fix items are addressed. (A severity-floor readiness verdict
— the D5 boundary proof: this fires nothing in calibration-honesty; the two vocabularies are disjoint.)

## An overridden note

The manuscript scores in the AI-elevated band, per the operator's transcription of SETEC's own worked
example.
<!-- override: calibration-honesty — quoting SETEC's documentation verbatim; not the consumer's own claim -->
