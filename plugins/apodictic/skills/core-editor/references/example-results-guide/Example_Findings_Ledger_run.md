# Findings Ledger (example stub)

Stub companion for the Results Guide fixture. Exists so the guide's citation of this
filename resolves under the `results-guide` validator's R2 check. Deliberately carries NO
apodictic:finding block — the guide fixture references it by NAME only (a navigation
pointer), and the guide itself must stay finding-block-free (R3).
