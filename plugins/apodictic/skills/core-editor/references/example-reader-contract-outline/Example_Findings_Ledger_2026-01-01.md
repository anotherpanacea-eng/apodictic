# Findings Ledger — Example

Canonical worked-example Findings Ledger for the reader-contract reverse outline. A Contract-Map gap
cell names one of these `finding_id`s (D4 — gap provenance is `finding_id`-only); the outline projects
the named finding verbatim into the `gap:` cell. `none logged` is a valid, non-degraded gap state.

## Pass — Promise-Contract Fidelity — Ledger Entry

### Notable Findings

1. **The town's secret is told, never shown.** The keeper's confession (S3) states the buried secret
   in dialogue but the manuscript never dramatizes its weight on the town, so the exposure half of the
   reader promise lands as information rather than reckoning.
   *(See Pass — Promise-Contract Fidelity, §Reveal Economy.)*

   <!-- apodictic:finding
   {
     "schema": "apodictic.finding.v1",
     "id": "F-PCF-02",
     "mechanism": "the town's buried secret is stated in the keeper's dialogue but never dramatized, so the exposure promise lands as information rather than reckoning",
     "severity": "Should-Fix",
     "confidence": "HIGH",
     "evidence_refs": ["S3"],
     "fix_class": "stage the reveal — let the secret cost someone something on the page",
     "risk_if_fixed": "a fuller reveal could slow the departure movement"
   }
   -->

### Data Artifacts for Letter Reference

- none

### Cross-Pass Connections

- none

### Unresolved Questions

- none

### Audit Triggers

| Trigger | Evidence | Recommendation |
|---------|----------|----------------|
| none | — | — |
