> **Macro block:** Character Architecture · **Writer question:** Are my characters landing? · **Legacy pass id:** Pass 5

# Pass 5 — Character Audit

_Canonical worked example: a Core DE pass artifact carrying the §3-sourced header
that `validate.sh pass-header` checks against `pass-dependencies.md §3`. The three
header values (Macro block, Writer question, Legacy pass id) are **read from §3**,
never authored — Pass 5's canonical block is Character Architecture, whose §3 User
Question is "Are my characters landing?". This fixture is the `--check-all` real-file
invariant for the `pass-header` arm; a §3 edit that drifts the block name, the
pass→block map, or the User Question breaks this check at release time._

## Notable Findings

- Protagonist agency holds across Acts I–II; the Act III turn is under-motivated
  (see the agency timeline in the data-artifacts section).
- Antagonist reads as a function rather than a person in Chapters 12–14.

## Data Artifacts for Letter Reference

- Agency timeline (per-scene decision ownership).
- Competence inventory (on-page demonstrations vs. asserted skill).

_(Illustrative body only — the validator checks the header, not this content.)_
